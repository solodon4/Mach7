name: Mach7 CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: cd code
      run:  cd code
    - name: make
      run:  make
    - name: make check
      run:  make check
